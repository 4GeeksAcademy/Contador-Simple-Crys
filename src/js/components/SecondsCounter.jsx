import React from "react";
import React, { useState, useEffect } from "react";
/**
 * Componente SecondsCounter
 * De momento es una maqueta vacía.
 * Luego le inyectaremos la lógica de conteo.
 */
export default function SecondsCounter() {
  return (
   const [numeroActual, setNumeroActual] = useState(0)
  );
}